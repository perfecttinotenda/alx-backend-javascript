/**
 * Will show a messge on STDOUT.
 * @param {String} msg the message to show.
 * @author P T Mashingaidze <https://github.com/perfecttinotenda>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
